package com.rudra.GUI_Swing.Lec1;

import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {
        //JFrame a GUI window to add components to
       /* MyFrame frame=new MyFrame();//creates new frame
        frame.setSize(420,420);//sets height and width
        frame.setVisible(true);//make frame visible
        frame.setTitle("Awesome Swing");//sets title of frame
        frame.setDefaultCloseOperation(MyFrame.EXIT_ON_CLOSE);//exit out of application
//        frame.setDefaultCloseOperation(JFrame.HIDE_ON_CLOSE);//hide out the application on close
//        frame.setDefaultCloseOperation(JFrame.DO_NOTHING_ON_CLOSE);//does nothing on close(never closes the application)
         frame.setResizable(false);//prevent from being resized
      ImageIcon image=new ImageIcon("C:\\IntelliJ\\DSA-JAVA\\src\\com\\rudra\\GUI_Swing\\logo.png");//create an image icon
      frame.setIconImage(image.getImage());
      frame.getContentPane().setBackground(new Color(123,50,250));//change color of the background
*/
        MyFrame frame=new MyFrame();
    }

}

